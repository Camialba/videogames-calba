const { getAllVideoGames } = require('../../src/clients/rawjClient.js');
const { expect } = require('chai');

describe('RawjClientTest', () => {
    test('GetAllGames', () => 
    it('first test',async () => {
        var res = await getAllVideoGames()
        console.log(res)
    }))
  });
